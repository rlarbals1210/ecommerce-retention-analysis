# data (원본은 외장 SSD 참조, 로컬엔 절대 커밋하지 않음)
data/
*.csv
*.csv.gz
*.parquet

# jupyter
.ipynb_checkpoints/

# python
__pycache__/
*.pyc
.venv/
venv/
env/
*.egg-info/

# os
.DS_Store

# reports 산출물 중 대용량은 제외하되 figures는 포함 (원하면 조정)

# 프로젝트 지시서 (레포 히스토리에는 남지만 앞으로는 추적 안 함)
CLAUDE.md
